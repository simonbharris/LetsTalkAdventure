capsule {
  id (lets_talk_of_adventure.gameFinder)
  version (0.1.8)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  
  capsule-imports {
    import (viv.entity) {
      as (entity)
      version (3.14.9)
    }
    import (viv.time) {
      as (time)
      version-from (entity)
    }
  }
}