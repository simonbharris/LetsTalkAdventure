// IGDB's call on the Game API returns a cover ID, which needs to be queried for the image URL
structure (Cover) {
  description (Cover image of a video game)
  property (id) {
    type (ID)
    min (Required) max (One)
  }
  property(image_id) {
    type (Image_ID)
    min (Required) max (One)
  }
}
