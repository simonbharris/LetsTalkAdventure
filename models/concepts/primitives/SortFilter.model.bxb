enum (SortFilter) {
  description (Primary filter to sort on. Select one)
  // lower case to more easily work with API
  symbol(popularity)
  symbol(total_rating) // top rated
  symbol(release_dates) // "newest"
}
